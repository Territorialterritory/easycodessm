<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.deceiver.dao.UserDao">

    <resultMap type="cn.deceiver.entity.User" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="pass" column="pass" jdbcType="VARCHAR"/>
        <result property="gender" column="gender" jdbcType="VARCHAR"/>
        <result property="addr" column="addr" jdbcType="VARCHAR"/>
        <result property="type" column="type" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select
          id, name, pass, gender, addr, type
        from test_database.user
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserMap">
        select
          id, name, pass, gender, addr, type
        from test_database.user
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserMap">
        select
        id, name, pass, gender, addr, type
        from test_database.user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="pass != null and pass != ''">
                and pass = #{pass}
            </if>
            <if test="gender != null and gender != ''">
                and gender = #{gender}
            </if>
            <if test="addr != null and addr != ''">
                and addr = #{addr}
            </if>
            <if test="type != null">
                and type = #{type}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into test_database.user(name, pass, gender, addr, type)
        values (#{name}, #{pass}, #{gender}, #{addr}, #{type})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into test_database.user(name, pass, gender, addr, type)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.pass}, #{entity.gender}, #{entity.addr}, #{entity.type})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into test_database.user(name, pass, gender, addr, type)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.pass}, #{entity.gender}, #{entity.addr}, #{entity.type})
        </foreach>
        on duplicate key update
        name = values(name) , pass = values(pass) , gender = values(gender) , addr = values(addr) , type = values(type)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update test_database.user
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="pass != null and pass != ''">
                pass = #{pass},
            </if>
            <if test="gender != null and gender != ''">
                gender = #{gender},
            </if>
            <if test="addr != null and addr != ''">
                addr = #{addr},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from test_database.user where id = #{id}
    </delete>

</mapper>